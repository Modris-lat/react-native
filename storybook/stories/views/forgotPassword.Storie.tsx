import React from 'react';
import {storiesOf} from '@storybook/react-native';
import { ForgotPassword } from '../../../src/views';

storiesOf('forgot password', module).add('default', () => <ForgotPassword />);