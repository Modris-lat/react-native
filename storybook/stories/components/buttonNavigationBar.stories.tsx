import React from 'react';
import {storiesOf} from '@storybook/react-native';
import { ButtonNavigationBar } from '../../../src/components';

storiesOf('button navigation bar', module).add('default', () => <ButtonNavigationBar />);